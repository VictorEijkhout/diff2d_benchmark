include ../../../makefiles/Make.clean
clean :: local_clean
total_clean ::
	@rm -f $$( make variants -f ../Makefile )
MAKEFILESDIR := ../../${MAKEFILESDIR}
MAKETRACESTRING := "${MAKETRACESTRING} .. .. .. "
